static class CreateWordTest
    {
        public static void Test(string[] wordsArray)
        {
            CentralStorage central = new CentralStorage();
            PoS partOfSpeech = new PoS("noun", central);
            
            foreach (var word in wordsArray)
            {
                Word nw = new Word(word, word, "-" + word.Substring(word.Length - 1), 
                    word.Reverse(), "a word", );
            }
        }
    }